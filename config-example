# this file should be placed in the same directory as the executable
# and named config.ini

[aranet]
# mac address of the aranet device
mac = 

[pushover]
# application token
token =
# user token 
user = 

[history]
# path to the history sqlite db
file = records.sqlite

# how timestamps should be stored in the db
# and presented to the user
date format = %Y/%m/%d %H:%M:%S